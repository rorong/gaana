<%= form_with(model: playlists_song) do |form| %>
  <% if playlists_song.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(playlists_song.errors.count, "error") %> prohibited this playlists_song from being saved:</h2>

      <ul>
        <% playlists_song.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :playlist_id, style: "display: block" %>
    <%= form.text_field :playlist_id %>
  </div>

  <div>
    <%= form.label :song_id, style: "display: block" %>
    <%= form.text_field :song_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
